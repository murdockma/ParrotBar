//
//  SettingsView.swift
//  ParrotBar
//
//  Created by michael on 8/4/24.
//

import Cocoa
import SwiftUI

struct HelpView: View {
    var body: some View {
        VStack {
            Text("Help")
                .font(.largeTitle)
                .padding()
        
            Spacer()
        }
        .padding()
        .background(Color(NSColor.windowBackgroundColor))
    }
}
